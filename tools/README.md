# tools

Scripts and utilities that are not part of the build and do not ship in the NuGet package.

Nothing lives here yet. Anything added should get a line in this file saying what it does and how
to run it.

Note that `BenchmarkTestBench/` is a solution project rather than a tool, because it references the
library and is run from Visual Studio.
