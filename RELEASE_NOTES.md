# RELEASE NOTES

## Version 3.0.0 (unreleased)

Changes are against version 2.2.0, the previous release on NuGet.

### Breaking Changes

* Targets .NET 8.0. Version 2.2.0 targeted .NET Standard 2.1, so projects on .NET Framework or .NET Standard cannot take this version.
* `GenericCache<TKey, TValue>` moved out of the global namespace into `CSharpExtender.Collections`. Consuming code needs `using CSharpExtender.Collections;`.
* `GenericCache.Remove` returns `bool` instead of `void`. Removing a key that is not in the cache returns `false` rather than throwing.
* `GenericCache` calculates expiration times in UTC instead of local time. Cached items no longer expire an hour early or late across a daylight saving change.
* `ObjectExtensionMethods.IsOfType<T>` and `IsNotOfType<T>` on an object are now an exact type match. An instance of a subclass of `T` no longer counts as being of type `T`. Use `IsOfTypeOrSubclass<T>` for the old behavior.
* `ObjectExtensionMethods.IsOfTypeOrSubclass` and `IsNotOfTypeOrSubclass` on a `Type` now include the type itself and, for an interface, its implementations. They previously used `Type.IsSubclassOf`, which excluded both, so `typeof(Foo).IsOfTypeOrSubclass<Foo>()` returned `false`.
* `JsonExtensionMethods.AsSerializedJson` and `StringBuilderExtensionMethods.AppendLineIfNotEmpty` each take a new optional parameter. Existing calls still compile, but they have to be recompiled.
* `DateTimeExtensionMethods.ToIso8601String` uses the round-trip ("O") format instead of `yyyy-MM-ddTHH:mm:ss.fffZ`. It previously appended a literal "Z" to every value, publishing a local or unspecified time as though it were UTC. A `Utc` value now ends in "Z", a `Local` value carries its real offset, and an `Unspecified` value carries neither. The fractional second is now seven digits rather than three, so the string changes for every caller.
* `StringExtensionMethods.IsDigitsOnly` returns `false` for a null string instead of `true`. An empty string still returns `true`.
* `StringExtensionMethods.SplitPascalCase` returns an empty list for a null or empty string. It previously returned a list holding that null or empty string, so a caller iterating the result got a null element.
* `StringExtensionMethods.SplitPath` no longer returns an empty entry for a path segment of nothing but whitespace. `@"a/ /b".SplitPath()` returned `["a", "", "b"]` and now returns `["a", "b"]`, which is what the method always documented. Segments are trimmed before empty ones are removed, rather than after.
* `EnumExtensionMethods.GetEnumValues<T>` returns a read-only collection instead of a new array, and hands the same instance to every caller. The declared return type is still `IEnumerable<T>`, so iterating it and running LINQ over it are unchanged, but casting the result to `T[]`, or writing to it through `IList<T>`, now fails. An enum's members are fixed when it is compiled, so a fresh writable copy per call bought nothing.
* `XmlExtensionMethods` parses attribute and element values under the invariant culture instead of the current one, so a document no longer means different things on different machines. `AttributeAsDateTime` also keeps what the text said about its time zone. Three things change. A slash-separated date is now always `MM/dd/yyyy`, so `15/09/2026` returns `default` where a European locale read it as 15 September, and `03/04/2026` is always 4 March where it was 3 April on those machines. A value ending in `Z` now comes back with `Kind` of `Utc` holding the time as written, where it previously came back as `Kind` of `Local` converted to the machine's offset. And `AttributeAsBool` now accepts `1` and `0` alongside `true` and `false`, in any casing, because `bool.ToString` writes `True` while schema-generated XML writes `1`, and reading only one of the two silently returned `false` for the other.
* `PropertyChangedLog.ChangeDateTime` is stamped in UTC instead of local time, so its `Kind` is `DateTimeKind.Utc` rather than `DateTimeKind.Local` and the value differs from before by the machine's offset. Code that displays it needs a `ToLocalTime()` it did not need before. Local time left the log ambiguous for the hour that repeats every autumn, where two entries stamped 01:30 can be an hour apart and the order they were written in is not recoverable. This matches the move `GenericCache` made for the same reason.
* `PropertyChangedLog.PropertyName` is never null, and the constructor throws `ArgumentNullException` for a null name rather than storing one. An entry that cannot say which property it is about records nothing, so reading the log no longer needs a null check. `PropertyChangeTrackingModel.SetProperty` reaches this only when a caller passes a property name explicitly, because `[CallerMemberName]` otherwise supplies one: such a call now adds no log entry instead of adding one with a null name, and the `PropertyChanged` notification it raises is unchanged. A null name is `INotifyPropertyChanged`'s "every property changed", which a log of individual property changes has no way to record.
* `MaskExtensionMethods.ToMaskedString` and `DisplayFormatExtensionMethods.ToDisplayString` no longer throw `AmbiguousMatchException` when a derived type hides a base property of the same name with `new`. They return a value instead, taking the first property of that name, which is what the reflection helpers in this library have always done. Which of the two that is depends on the order `Type.GetProperties` returns them in, and .NET does not guarantee one, so a type with a hidden property should name the two properties differently rather than rely on which one is found. Nothing else changes: a type without a hidden property behaves exactly as it did.

### Features

New classes:

* `SmartReflection` and `SmartReflectionExtensionMethods` - reflection helpers that cache each type's properties, so repeated lookups do not re-enter `Type.GetProperties`.
* `JsonRedactionService` and `XmlRedactionService` - remove sensitive values from JSON and XML, for paths matching a list of regex patterns. Both expose the same four members, `Redact` and `RedactToString` over the document type and over text.
* `CompositeRegexMatcher` - checks a string, or a span, against a list of regex patterns compiled into one. `HasPatterns` says whether there is anything to match at all. `BaseRedactionService` holds one of these and exposes it to its subclasses as the protected `_matcher` field, rather than repeating the pattern-combining logic.
* `VersionExtensionMethods` and `SemanticVersion` - read a version number out of a string, keeping the alpha text rather than stripping it, so `1.2.3-beta.1`, `2.0.0-rc1` and `3.1.0+build7` all read. `ToSemanticVersion` throws `FormatException` on text it cannot read and `TryParseSemanticVersion` returns false, dividing the work the way `Version.Parse` and `Version.TryParse` do; neither returns a zero version for input it could not read. `NumericPartOfVersion` and `PrereleaseLabelOf` answer in strings for callers that want one piece. `SemanticVersion` is an immutable record holding the numeric parts, the label and the metadata together, because `System.Version` has no room for a label and would compare `1.2.3-beta.1` as equal to `1.2.3`. It orders by the Semantic Versioning precedence rules through `IComparable<SemanticVersion>` and the comparison operators, so a prerelease sorts before the release it leads to and `1.0.0-beta.2` sorts before `1.0.0-beta.11`. Build metadata takes no part in precedence, while equality covers it.
* `XElementExtensionMethods` - the `XmlExtensionMethods` set over `XElement`, the LINQ to XML API that new code uses, mirroring it member for member: `AttributeAsString`, `AttributeAsInt`, `AttributeAsBool`, `AttributeAsDateTime`, `ElementAsString` and `ElementAsInt`, each answering exactly what its `XmlNode` twin answers for a missing attribute, a missing element and a value that will not parse. Adds `GetValue`, which returns whichever of an attribute or a child element of that name carries the value, for XML that writes the same value either way. The one difference from the `XmlNode` set is that a null receiver throws `ArgumentNullException` rather than `NullReferenceException`.
* `DataReaderExtensionMethods` - reads a column out of an `IDataReader` by name rather than by ordinal, which is the forward-only path used for anything too large to pull into a `DataSet`. `GetValue<T>` plus typed wrappers for `string`, `int`, `bool`, `decimal`, `double`, `float`, `DateTime`, `Guid` and `byte[]`. A `DBNull` column returns `default`, a nullable type is converted to its underlying type so a null column read as `int?` returns null, and a column storing a `Guid` as text is parsed. A name that is not in the result set throws `IndexOutOfRangeException` naming it and listing the columns that are there, normalized across providers that throw `ArgumentException` instead. A value that will not convert throws `InvalidCastException` naming the column.
* `AlphaOnlyAttribute` - validates that a string property contains only letters. A letter is whatever `char.IsLetter` accepts, so a letter outside the ASCII range passes and a space does not. A null or empty value passes, leaving presence to `[Required]`. It validates strings, and throws `InvalidCastException` for a member of any other type, as `NumericOnlyAttribute` does, because what would otherwise be checked is whatever the member's type prints, which is a formatting decision rather than the value: an enum member would pass on the name it happens to be spelled with, and a `Guid` could never pass at all.
* `ConditionalRequiredAttribute` - validates that a property has a value when another property on the same object holds a particular value. Needs a call that supplies the object, such as `Validator.ValidateObject`.
* `IsInListAttribute` - validates that a property holds one of a fixed list of allowed values, optionally ignoring case for strings.
* `MaskAttribute` - declares how a property's value should be masked when displayed or logged. Not a `ValidationAttribute`: `MaskExtensionMethods` applies it.
* `MaskExtensionMethods` - `ToMaskedString` and `ToMaskedStrings` apply a property's `MaskAttribute`.
* `DisplayFormatExtensionMethods` - `ToDisplayString` and `ToDisplayStrings` apply the `DisplayFormatAttribute` from `System.ComponentModel.DataAnnotations`, which is otherwise only honored by UI frameworks. Accepts both a bare format specifier and the conventional `"{0:...}"` form.
* `NumericOnlyAttribute` - validates that a string property holds a number and nothing else, with `AllowDecimal`, `AllowThousandsSeparator`, `AllowCurrencySymbol` and `AllowNegative` each turning on one more thing the text may hold, and no leading or trailing whitespace in any of them. With no option set it checks characters, each one an ASCII digit, which brings in no culture and puts no limit on how many digits there are. This is stricter than `StringExtensionMethods.IsDigitsOnly`, which accepts any Unicode decimal digit, so that the default stays a subset of what the options accept. With any option set the text is read as a number under `CultureInfo.CurrentCulture`, so which separators a value may use is the culture's answer rather than the attribute's, and `UseInvariantCulture` reads it the same way everywhere instead. Reading it as a number brings `decimal`'s ceiling with it, so a run of more than 29 digits passes with no option set and fails with one set: "every character is a digit" and "this reads as a number" are different questions and they part company there. A null or empty value passes, leaving presence to `[Required]`, which is what `AlphaOnlyAttribute` does. It validates strings, and throws `InvalidCastException` for a member of any other type, because a numeric member cannot hold text that is not a number.
* `UniqueItemsAttribute` - validates that a collection property contains no duplicates.
* `StringBuilderOptions` - controls case, indentation, maximum length, prefix and suffix text, HTML escaping, and formatting for the StringBuilder extension methods.

The package ships XML documentation. `CSharpExtender.xml` is packed beside the assembly, so IntelliSense shows the summary, parameter text and remarks for a member instead of its bare signature. Every public and protected member carries a comment, and the build warns about any that does not, so a new member cannot ship undocumented without somebody ignoring a warning to do it.

The library is annotated for nullable reference types, so every public signature now says which arguments accept null and which returns can be one. Nothing about what the code does changes, and nothing stops compiling: a consumer with nullable reference types enabled sees new warnings where it was passing a null the library does not accept, or dereferencing a return that can be one. A consumer with them switched off sees nothing. The annotations worth knowing about:

* `GenericCache.Get` returns `TValue?`, and `TryGet` marks its `out` parameter as null when it returns false, so both say what a missing key gives back. `ObjectExtensionMethods.DeepClone`, `JsonExtensionMethods.AsDeserializedJson` and `GetValueFromJsonPath`, `StringExtensionMethods.ConvertFromString`, `LinqExtensionMethods.RandomElement`, `DataReaderExtensionMethods.GetValue`, `DataSetExtensionMethods.Get`, and the `SmartReflection` members that read a property or invoke a method likewise return a nullable type, because each of them can hand back a null or a `default`.
* `StringExtensionMethods.HasText`, `DoesNotHaveText` and `IsDigitsOnly` tell the compiler what their answer proves about the string, so a `true` from `HasText` is enough for the caller to use the value without a further null check.
* `Mask`, `ToMaxLengthOf` and `CollapseWhitespace` are declared as returning null only for a null argument, so passing a string that is known not to be null gives back one that is known not to be null.
* `SemanticVersion.PrereleaseLabel` and `BuildMetadata`, and `PropertyChangedLog.NewValue`, are nullable. Each of them was already null in the cases the documentation describes. `PropertyChangedLog.PropertyName` is not: see Breaking Changes.

New members:

* `StringBuilderExtensionMethods` gains `Append`, `AppendIf`, `AppendLine`, `AppendLineIf`, `AppendFormatted`, `AppendLineFormatted`, `AppendJoined` and `AppendLineJoined`. Every method, including the existing `AppendLineIfNotEmpty`, accepts an optional `StringBuilderOptions`. `AppendFormatted` and `AppendLineFormatted` take theirs as an overload rather than an optional parameter, because an optional parameter in front of a `params` array has to be filled in: `sb.AppendFormatted("{0} of {1}", current, total)` reads as it should, and the options go in the same position when there are any.
* `GenericCache.TryGet` - retrieves a value without returning `default` for a key that is genuinely absent.
* `GenericCache` takes an optional cleanup interval, through a second constructor, and drops expired entries on a thread pool timer at that interval. A tick that arrives while the previous pass is still running is dropped rather than queued. The constructor that takes no interval starts no timer, so a cache built the way every existing one is built behaves exactly as it did, and expired entries are dropped as they are read either way. This is for the entries nobody reads again.
* `GenericCache` implements `IDisposable`, and a cache given a cleanup interval has to be disposed. The timer holds a reference to the cache, so until it is disposed the cache cannot be collected and the pass keeps running. Disposing a cache that was given no interval does nothing, and disposing twice does nothing the second time.
* `NumericExtensionMethods.ApproximatelyEquals` - compares two floats within a percentage tolerance, handling `NaN`, infinity, and values near zero.
* `StringExtensionMethods.ToMaxLengthOf` - trims a string to a maximum length, leaving a shorter string and a null alone.
* `EnumExtensionMethods.GetEnumDisplayName` and `GetEnumDisplayNames<T>` - read the `Name` from a member's `[Display]` attribute, which is what data-annotation-driven UI binds to, where `GetEnumDescription` reads `[Description]`. Both fall back to the value's string representation when the member has no `[Display]`, its `[Display]` sets no `Name`, or the value is undefined or a combination of `[Flags]` members. They cache per closed enum type the way the description pair does, and read independently of it, so a member carrying both attributes gets the right one from each.
* `StringExtensionMethods.CollapseWhitespace` - trims a string and reduces every run of whitespace inside it to a single space, tabs and line breaks included. A null returns null, a string of nothing but whitespace returns empty, and text that needs nothing changed comes back as the same instance rather than a copy.
* `StringExtensionMethods.Mask` - replaces the middle of a string with a mask character, leaving a number of characters visible at each end. Separators stay visible by default, so a card or phone number keeps its shape.
* `NumericExtensionMethods.StandardDeviation` and `PopulationStandardDeviation` - sample (n-1) and population (n) standard deviation of a collection, with overloads for `double`, `int` and `decimal`.
* `JsonExtensionMethods.AsSerializedJson` accepts an optional `JsonSerializerOptions`.
* `StringExtensionMethods.IncludesTheWords` gains an overload taking a `StringComparison` before the words. The existing signature keeps comparing with `CurrentCultureIgnoreCase`, so nothing has to change; `Ordinal` and `OrdinalIgnoreCase` measure 12x to 20x faster.

### Bug Fixes

* `JsonExtensionMethods.PrettyPrintJson(object, JsonSerializerOptions)` copies the supplied options instead of setting `WriteIndented` on the caller's instance. It previously threw `InvalidOperationException` for any options instance that had already been used to serialize, and silently left `WriteIndented` set to `true` on the caller's object.
* `EnumExtensionMethods.GetEnumDescription` returns the value's string representation instead of throwing `NullReferenceException` for an undefined value or a combination of `[Flags]` members.
* `StringBuilderOptions.MaxLength` caps the value rather than the raw text it was handed. It runs after `ToUpper`, `ToLower` and `EscapeHtml` have rewritten the value, and before `Format`, `PrefixText`, `SuffixText` and the indent wrap it. It previously ran before all of those, so every option applied afterwards pushed the result back past the limit: a `MaxLength` of 3 with a `PrefixText` of `[` and a `SuffixText` of `]` returned five characters against a limit of three. The result can still be longer than `MaxLength`, because the four wrapping options are the caller's own text rather than the value, and cutting into them would lose the closing half of a format or a suffix. What changes is that the cap now counts the text case and HTML escaping produced, instead of what came in.
* `JsonRedactionService.Redact(JsonObject)` and `RedactToString(JsonObject)` throw `ArgumentNullException` for a null argument, which is what `XmlRedactionService` already did. `Redact` returned null and `RedactToString` threw `NullReferenceException`, so the two services answered a null argument differently, and the `NullReferenceException` read as a defect inside the service rather than a bad argument. The string overloads throw `ArgumentException` for text that is the JSON literal `null`, which is valid JSON but not a document there is anything to redact in; `Redact` returned null for it and `RedactToString` threw `NullReferenceException`.

### Performance

Measured with BenchmarkDotNet. No behavior changes: every method below returns what it returned before.

* `StringExtensionMethods.IsDigitsOnly` iterates a `ReadOnlySpan<char>` instead of running a LINQ predicate over the string.
* `StringExtensionMethods.Repeat` builds the result with `string.Create`, and uses `new string(char, int)` when the text is a single character, instead of concatenating an `Enumerable.Repeat`. Between 1.6x and 11x faster depending on the text and count, with the single-character case seeing the largest gain.
* `StringExtensionMethods.ToDigitsOnly` filters into a stack buffer instead of a LINQ `Where` and `ToArray`. Between 2.7x and 4.3x faster, allocating a sixth as much for short strings.
* `StringExtensionMethods.IncludesTheWords` searches a `ReadOnlySpan<char>` and no longer uses LINQ. Around 5% faster, and no longer allocates.
* `StringExtensionMethods.RemoveText` tests whether a pass shortened the string instead of calling `Contains` before each `Replace`, saving a scan of the string per pass.
* `EnumExtensionMethods.GetEnumDescription` holds its cache per closed enum type instead of in one dictionary keyed on a boxed `Enum`. Between 10x and 18x faster, and no longer allocates on a cache hit. `GetEnumDescriptions` benefits through it.
* `ObjectExtensionMethods.DeepClone` reuses one `JsonSerializerOptions` instead of building one per call, and round-trips through UTF-8 bytes rather than a string. About 30% faster, allocating 15% less, and no longer reaching gen 1 or gen 2.
* `StringBuilderExtensionMethods` share one default `StringBuilderOptions` instead of allocating one whenever the caller passes none, and `AppendLineIfNotEmpty` tests its condition directly instead of through a `Func<bool>` that captured the text. `AppendLineIfNotEmpty` is 3.5x faster; both stop allocating.
* `GenericCache.Set` writes through the dictionary indexer instead of `AddOrUpdate` with a lambda that captured the new item. About 29% faster, allocating a quarter as much.
* `StringExtensionMethods.SplitPath` holds its separator array in a static field instead of building one per call, and splits with `StringSplitOptions.TrimEntries` rather than trimming through LINQ afterwards. See Breaking Changes: that also corrected the whitespace-only segment case.
* `StringExtensionMethods.SplitPascalCase` uses a source-generated regex instead of the static `Regex.Replace`, which looked the pattern up in the process-wide regex cache and then ran it interpreted. Between 3.5x and 7.6x faster. The source generator is one of the things .NET Standard 2.1 could not use.
* `StringExtensionMethods.Mask` holds the positions of the maskable characters in a stack buffer instead of a `List<int>`. Between 1.2x and 1.5x faster, allocating half as much.
* `NumericExtensionMethods.StandardDeviation` and `PopulationStandardDeviation` cast their `int` and `decimal` values straight to `double`. Both overloads shared a generic helper that converted through `Convert.ToDouble(object)`, boxing every element. Between 1.7x and 1.9x faster, allocating a quarter as much for a thousand values.
* `SmartReflection` finds a property with a loop over its cached array rather than `FirstOrDefault`, whose predicate captured the property name into a closure and allocated a delegate on every lookup. `GetPropertyValue` is 1.9x faster and no longer allocates; `SetPropertyValue`, `GetPropertyType`, `HasProperty` and `HasPropertyAttribute` go through the same lookup. The `SmartReflectionExtensionMethods` wrappers benefit through them.
* `SmartReflection.InvokeMethod` keys its method cache on a struct holding the type, method name and argument types instead of a string built by interpolating and joining all of those together. Building that key cost several times what the reflection call the cache exists to avoid costs. 4.1x faster, allocating a sixth as much.
* `UniqueItemsAttribute` no longer re-reads an item's properties for every comparison that item takes part in. It reads a property the first time a comparison reaches it and remembers it, so a list costs one read per property actually compared rather than one per pair. For 200 objects that have to be compared all the way down, 8.4x faster and allocating 106x less, because a value-typed property is no longer boxed afresh on every one of the 19,900 comparisons.
* `UniqueItemsAttribute` answers a list of 16 or more strings or value types in one pass through a `HashSet` instead of comparing every pair. 21.6x faster for 200 strings and 15x for 200 of a user defined struct, for a few KB more allocated. Three things stay on the pairwise walk. Lists shorter than 16, because building the set costs more than the comparisons it saves below that length. Classes, because the attribute compares them property by property rather than by their own `Equals`, so a set would answer a different question. And any value type that overrides `Equals` or `GetHashCode` without the other, because a set finds duplicates by hash first, so a type whose equal values hash differently would have its duplicates go unseen. A value type that overrides both, or neither, is taken at its word. The one-pass check only decides whether the pairwise walk has to run, so the indexes named in the error message do not change.
* `MaskExtensionMethods` and `DisplayFormatExtensionMethods` hold each type's attributed properties instead of reading the type's properties, and every property's attribute, on every call. `ToMaskedStrings` is 13.4x faster, `ToDisplayStrings` 10.5x, and `ToMaskedString` and `ToDisplayString` 8.6x, each allocating about a third as much.
* `StringExtensionMethods.ConvertFromString` holds the `TypeConverter` per type instead of asking `TypeDescriptor` for it on every call. 5.7x faster for an `int` and 1.8x for a `DateTime`. `JsonExtensionMethods.GetValueFromJsonPath` benefits through it.
* `ConditionalRequiredAttribute` looks its dependent property up through the same cache, rather than calling `Type.GetProperty` on every validation.
* `EnumExtensionMethods.GetEnumValues` holds its values per enum type instead of building a new array on every call. Fetching them is 78x faster and no longer allocates, and iterating them is 4.7x faster. `GetEnumDescriptions` benefits through it. See Breaking Changes for the change to what it hands back.
* `SmartReflection` and `UniqueItemsAttribute` share that one property cache rather than keeping a copy each.
* `XmlExtensionMethods.ElementAsString` and `ElementAsInt` find a plain element name by walking the node's children instead of going through `SelectSingleNode`, which parses the name as an XPath expression on every call. 11.2x faster, and it no longer allocates. An argument that is not a plain element name, such as `"contact/email"`, still goes to `SelectSingleNode`, so an expression that works today keeps working.
* `JsonRedactionService` and `XmlRedactionService` build the path to the node they are looking at in one buffer that is reused for the whole document, and match it as a span, instead of interpolating a new string at every level for every node. They also hold off allocating their working lists until there is something to put in them. Redacting a 100 record document allocates 16% less for JSON and 20% less for XML. The walk itself, measured against the same document with no patterns to match, is 1.16x faster for JSON and 1.30x for XML, allocating 23% and 44% less. Parsing and re-serializing the document is most of what is left.
* `JsonExtensionMethods.GetValueFromJsonPath` walks the path as spans rather than splitting it into an array of strings first. `JsonElement` can be asked for a property by span, so the segments never have to exist. Allocates 104 bytes where it allocated 256, and is around 1.1x faster.
* `StringBuilderExtensionMethods` apply indentation, prefix and suffix in one pass. They were three separate concatenations, each building another string, plus a fourth for the indent itself. With all three set, 1.9x faster and allocating a third as much. Text with none of them set is unaffected.
* `ObservableModel.SetProperty` compares through `EqualityComparer<T>.Default` instead of the static `object.Equals`, which boxed both the backing field and the new value for a value-typed property, on every set, including the sets that turn out to change nothing. Between 1.4x and 1.8x faster, and a set that changes nothing no longer allocates at all. `PropertyChangeTrackingModel` benefits through it.
* `JsonExtensionMethods.PrettyPrintJson` reuses one `JsonSerializerOptions` rather than building one per call, which threw away the converter and type metadata System.Text.Json caches on the instance. 1.5x faster, allocating a third less, and it no longer reaches gen 1 or gen 2. The overload taking a caller's options still copies them, so it does not write to the instance it was handed.
* `UniqueItemsAttribute` copies the collection it is validating into a list of the right size, rather than through `Cast<object>().ToList()`, whose iterator cannot report a count so the list grew by doubling and copied as it went. Between 1.6x and 2.0x faster for strings and 2.4x to 3.1x for numbers, allocating about half as much.
* `IsInListAttribute` tests its allowed values with a loop rather than `Any`, whose predicate captured both the value being validated and the attribute, so every validation allocated a closure and a delegate. 3.6x faster and no longer allocates when the value is in the list.
* `StringBuilderExtensionMethods.AppendJoined` and `AppendLineJoined` walk a lazy source once. They tested it with `Any()` and then handed the same sequence to `string.Join`, so a source that can only be enumerated once was read twice.

### Dependencies

* The package has no NuGet dependencies. Version 2.2.0 referenced `System.Text.Json` 8.0.4, which .NET Standard 2.1 needed. .NET 8.0 carries `System.Text.Json` in its shared framework, so the reference has been dropped rather than updated. Consuming projects now use whichever version their own runtime supplies, and pick up its servicing updates, instead of being held to the one this package was built against.
